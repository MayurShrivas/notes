function square (x) {
    let sq = x * x;
    return sq;
}
let returnValue = square (4);
console.log (returnValue);

let power = Math.pow (2,3);
console.log (power);

// Math.pow is used for cube root of any number.
// Math.sqrt is used for Square root of any number.
