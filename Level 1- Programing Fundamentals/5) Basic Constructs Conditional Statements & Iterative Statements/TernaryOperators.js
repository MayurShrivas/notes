/* Write a program to take two varibales - a and b.
Output 1 - If a is less than b. 
Output 0 - If a is more than b */

let a = 51, b = 6;

let c = (a < b) ? 1 : 0;

console.log (c);

// Ternary Operator is a shortcut for ifelse if.
