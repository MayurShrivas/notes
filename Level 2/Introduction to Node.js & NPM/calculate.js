const { subtractExported } = require ('./Lib/math'); //To import call function. (Incase of diffrent file only change file path in libu like ('./lib/filepath);)  
console.log (subtractExported (2, 3));

